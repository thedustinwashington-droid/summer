# Summer Parenting Scheduler

A static HTML/CSS/JavaScript app for drafting and saving summer parenting-time calendars.

## Run locally

Open `index.html` directly in a browser, or serve the folder with any static file host.

## GitHub Pages

This repo is static-only and ready to publish with GitHub Pages from the default branch root.
